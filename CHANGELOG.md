# CHANGELOG

## [Unreleased]

### Added
- Added publication-style task flow figure outputs and reproducibility artifacts:
  - `task_flow.png`
  - `references/task_plot_spec.yaml`
  - `references/task_plot_spec.json`
  - `references/task_plot_source_excerpt.md`
  - `references/task_plot_audit.md`

### Notes
- Task plot is now generated by the `skills/task-plot` workflow with local-first layout QA.

### Changed
- Removed the Sternberg task-local controller and replaced it with pure helper-based trial sampling plus native `BlockUnit.generate_conditions(...)` scheduling.
- Moved deterministic trial sampling to `task.overall_seed` / block seeds and kept cumulative score tracking in minimal runtime state.
- Promoted task maturity from `draft` to `smoke_tested` after passing local validation, QA, and simulation gates.
- Expanded README coverage for paradigm rationale, implementation notes, and review/output status.

## [0.1.0] - 2026-03-04

### Added
- Initial Sternberg Working Memory task scaffold (`T000035`).
- Mode-aware runtime in `main.py` for `human|qa|sim`.
- Sternberg trial implementation in `src/run_trial.py` with `memory_set`, `retention`, `probe_response`, `feedback`, and `inter_trial_interval` phases.
- Trial controller in `src/utils.py` for memory/probe sampling and score/accuracy tracking.
- Config suite: `config/config.yaml`, `config/config_qa.yaml`, `config/config_scripted_sim.yaml`, `config/config_sampler_sim.yaml`.
- Sampler responder plugin in `responders/task_sampler.py`.
- Initial docs/metadata and reference artifacts.

### Changed
- None.

### Fixed
- None.
